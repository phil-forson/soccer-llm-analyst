# HTTP requests
requests>=2.31.0

# Environment variable management
python-dotenv>=1.0.0

# OpenAI API client (LLM)
openai>=1.0.0

# Web search (DuckDuckGo)
ddgs>=7.0.0

# YouTube API
google-api-python-client>=2.0.0

# HTML parsing
beautifulsoup4>=4.12.0

# Semantic similarity for ranking (web search + YouTube highlights)
# Optional: if not installed, falls back to keyword-based ranking
sentence-transformers>=2.2.0
numpy>=1.24.0

# FastAPI for REST API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Testing (optional)
pytest>=7.0.0
